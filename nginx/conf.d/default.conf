server {
    listen 80;
    server_name _;

    client_max_body_size 100M;

    # Pull in all microservice routes
    include /etc/nginx/conf.d/services/*.conf;

    # Default 404 for unknown services
    location / {
        default_type text/plain;
        return 404 "Service Not Found";
    }
}
