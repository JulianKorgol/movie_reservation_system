server {
    listen 80;
    server_name _;

    client_max_body_size 100M;

    # Pull in all microservice and frontend routes.
    # frontend-main-app.conf defines location / as the catch-all.
    include /etc/nginx/conf.d/services/*.conf;
}
