# PostgreSQL credentials for the IAM service database.
# Copy this file to envs/.env.postgres-iam and fill in real values.
# Never commit the non-example file — it is listed in .gitignore.

POSTGRES_USER=iam_user
POSTGRES_PASSWORD=CHANGE_ME_strong_password
POSTGRES_DB=iam_db
