# Runtime configuration for the nginx reverse-proxy.
# Copy this file to envs/.env.nginx and fill in real values.
# Never commit the non-example file — it is listed in .gitignore.

# nginx reads no secrets at runtime in the current config.
# Add variables here when/if envsubst templating is introduced
# (e.g. NGINX_CLIENT_MAX_BODY_SIZE, TLS cert paths, etc.)
